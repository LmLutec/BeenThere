<h2><%= by_location(@location) %></h2> </br></br>
<section id= "ratings">
<b>Satisfaction rating:</b> <%= avg_satisfied(@location) %>
<b>Chance of revisit:</b> <%= avg_revisit(@location) %>

<b>Reviews available:</b> <%= @location.reviews.count%></br>
</section>


    <% @location.reviews.order(:occasion).each do |review|%></br>
        <details>
          <summary> <%= "Occasion: #{review.occasion}" %></summary></br>
                    <%= "Satisfaction: #{review.satisfaction}" %></br>
                    <%= "Comments: #{review.comments}" %></br>  
                    <%= "Revisit: #{review.revisit}" %></br>
                    <%= "Stay Length: #{review.stay_length}" %></br>
                    <%= "Living: #{review.living}" %></br>
                    <%= "Suggestion: #{review.suggest}" %></br>
                    <%= "Food rating: #{review.food_rating}" %></br>
                    <%= "Events: #{review.events}" %></br>
                    <%= "Cost level: #{review.cost_level}" %></br>
        </details>            
                    <% end %></br></br>
    
    
        
   
    
<p>Heading for occasion and other attributes</p>

    
        


    
    <p>comment count</p>
    <p>comments</p>
